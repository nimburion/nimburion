# `pkg/observability/config`

Family-owned configuration section for `pkg/observability`.

Owns observability config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
