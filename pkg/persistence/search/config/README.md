# `pkg/persistence/search/config`

Family-owned configuration section for `pkg/persistence/search`.

Owns search config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
