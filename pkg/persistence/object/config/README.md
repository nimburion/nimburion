# `pkg/persistence/object/config`

Family-owned configuration section for `pkg/persistence/object`.

Owns object-storage config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
