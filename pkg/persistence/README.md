# `pkg/persistence`

Shared persistence family root.

Groups role- and storage-model-specific packages such as `relational`, `document`, `object`, `search`, and `keyvalue` without collapsing them back into one umbrella API.
