# `pkg/http/securityheaders/config`

Family-owned configuration section for `pkg/http/securityheaders`.

Owns security-header config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
