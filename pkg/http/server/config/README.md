# `pkg/http/server/config`

Family-owned configuration section for `pkg/http/server`.

Owns public HTTP and management server config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
