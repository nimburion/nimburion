# `pkg/http/httpsignature/config`

Family-owned configuration section for `pkg/http/httpsignature`.

Owns HTTP signature config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
