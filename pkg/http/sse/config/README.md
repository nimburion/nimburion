# `pkg/http/sse/config`

Family-owned configuration section for `pkg/http/sse`.

Owns server-sent event config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
