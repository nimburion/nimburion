# `pkg/http/middleware/testutil`

Test helpers shared by HTTP middleware packages.

Owns middleware-family mocks and package-local test support only.

Non-goals: production runtime behavior.

Testing: consumed by package-local tests across `pkg/http/middleware/*`.

Status: target-state HTTP test-support package.
