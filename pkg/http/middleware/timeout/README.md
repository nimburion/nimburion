# `pkg/http/middleware/timeout`

HTTP request-timeout middleware for the HTTP family.

Owns request-scoped timeout policy for HTTP handlers.

Testing: fast tests cover timeout enforcement and path-policy behavior.

Status: target-state HTTP package.
