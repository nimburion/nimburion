# `pkg/http/openapi/config`

Family-owned configuration section for `pkg/http/openapi`.

Owns OpenAPI and Swagger config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
