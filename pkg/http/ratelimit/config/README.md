# `pkg/http/ratelimit/config`

Family-owned configuration section for `pkg/http/ratelimit`.

Owns rate-limit config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
