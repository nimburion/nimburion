# `pkg/eventbus/schema/config`

Family-owned configuration section for `pkg/eventbus/schema`.

Owns schema-validation config types plus defaults, env binding, and validation contributor hooks consumed by `pkg/config`.
