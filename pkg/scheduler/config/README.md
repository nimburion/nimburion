# `pkg/scheduler/config`

Family-owned configuration section for `pkg/scheduler`.

Owns scheduler config types plus defaults, env binding, validation, and scheduler-local task validation consumed by `pkg/config`.
