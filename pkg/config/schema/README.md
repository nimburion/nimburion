# `pkg/config/schema`

Composed configuration schema root.

Builds the effective schema from family-owned config extensions and core wiring rules. This package replaces the old `pkg/configschema` ownership model.
