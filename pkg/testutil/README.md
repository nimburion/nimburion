# `pkg/testutil`

Shared test-support package for public framework packages.

Owns reusable helpers that are intentionally safe to import from tests across package families without leaking production wiring concerns.
